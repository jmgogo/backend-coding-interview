meta {
  name: Create Photographer
  type: http
  seq: 2
}

post {
  url: {{v1}}/photographers
  body: json
  auth: inherit
}

body:json {
  {
    "name": "test",
    "profileUrl": "example.com"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
